# Build the core utils libraries used by the rest of the HSSH and then create each of those
# individual libraries and their associated modules

subdir('utils')
subdir('metrical')
subdir('local_metric')
subdir('global_metric')
subdir('local_topological')
subdir('global_topological')
